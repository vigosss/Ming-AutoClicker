<UserControl x:Class="Ming_AutoClicker.Views.MacroListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:Ming_AutoClicker.Models">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 宏列表 -->
        <ListBox Grid.Row="0"
                 x:Name="MacroListBox"
                 ItemsSource="{Binding Macros}"
                 SelectedItem="{Binding SelectedMacro}"
                 Style="{StaticResource ModernListBox}">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource CardListBoxItem}"/>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type models:MacroProfile}">
                    <Grid Height="52">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- 宏信息 -->
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Medium"
                                       Foreground="{StaticResource TextPrimaryBrush}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Style="{StaticResource CaptionText}" Margin="0,4,0,0">
                                <Run Text="{Binding Actions.Count, Mode=OneWay, StringFormat='{}{0} 个动作'}"/>
                                <Run Text=" · "/>
                                <Run Text="{Binding UpdatedAt, StringFormat='{}{0:MM-dd HH:mm}'}"/>
                            </TextBlock>
                        </StackPanel>

                        <!-- 操作按钮（仅选中时显示） -->
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <Button Content="✏️ 编辑" Style="{StaticResource GhostButton}" Margin="0,0,4,0"
                                    Click="OnEditClick" Tag="{Binding}"
                                    IsEnabled="{Binding DataContext.IsNotExecuting, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                            <Button Content="📋 复制" Style="{StaticResource GhostButton}" Margin="0,0,4,0"
                                    Command="{Binding DataContext.DuplicateMacroCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                            <Button Content="🗑 删除" Style="{StaticResource DangerButton}"
                                    Command="{Binding DataContext.DeleteMacroCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- 创建新宏按钮 -->
        <Button Grid.Row="1"
                Content="＋ 创建新宏"
                Style="{StaticResource PrimaryButton}"
                Height="38"
                Margin="0,12,0,0"
                Command="{Binding CreateMacroCommand}"/>

        <!-- 执行控制 -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="选中宏后点击开始或按 F8 运行"
                       Style="{StaticResource CaptionText}"
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1"
                    Content="▶ 开始"
                    Style="{StaticResource SuccessButton}"
                    Width="80" Height="36" FontSize="13"
                    Margin="0,0,8,0"
                    Command="{Binding StartMacroCommand}"/>
            <Button Grid.Column="2"
                    Content="■ 停止"
                    Style="{StaticResource DangerButton}"
                    Width="80" Height="36" FontSize="13"
                    Command="{Binding StopMacroCommand}"/>
        </Grid>
    </Grid>
</UserControl>